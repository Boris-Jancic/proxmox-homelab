---
- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: true

- name: Ensure nginx is enabled and running
  systemd:
    name: nginx
    enabled: true
    state: started
