all:
  hosts:
    localhost:
      ansible_connection: local
    # pve:
    #   # REPLACE with your Proxmox node IP/hostname if remotely configuring
    #   ansible_host: 192.168.88.10
    #   ansible_user: root
